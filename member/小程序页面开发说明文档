首页：
    小程序首页为funclist。页面加载后会调用小程序登录接口，然后获取服务端所有枚举信息缓存到小程序，页面中可以调
    用this.$getEnumName('枚举名称', '枚举值')来获取枚举名称，示例查看员工列表。
    ---页面启动逻辑
        1、如果页面是正产启动的会请求服务器接口，根据服务器返回进入不同的页面（注册用户或到工作台）。
        2、如果是用户从其他分享链接进入，那么页面会取到target值，完成调用初始化接口好，跳转到分享链接要进入的页面。

用户注册：
    用户注册目前分两种情况
        1.受邀请注册，页面会从首页跳转过来并且会带上参数invited和员工ID，页面会根据员工ID接口获取公司信息。
        2.普通注册，页面会从首页根据服务器接口跳转到该页面（没有参数），注册成功后和受邀请注册跳转页面不同。

注册公司:
       公司注册页面有文件上传功能，先上传公司所需图片，等完成后再提交公司信息。表单校检和文件上传示例可以参考该页面。

工作台：
    页面进入后去加载岗位信息接口（存储到小程序缓存，当前页面和后续页面都要使用）、当前公司详情接口、当前用户信息
    接口（根据接口是否返回公司信息判断是否拥有公司展示不同）

员工：
    员工列表页面
        该有上拉刷新和下拉加载更多的实例，分页功能目前也只有在该页面实现。后端返回的数据枚举值用$getEnumName方法处理过。


    员工邀请页面
        员工列表页面点击分享链接跳转到该页面，点击邀请页面，会吊起分享功能，分享的链接为会用中心首页加target参数，参数值为
        经过encodeURIComponent方法编码过的要跳转的地址。

    员工编辑和添加页面：
        根据页面是否带有员工ID判断是否是为编辑（如果是编辑，加载员工信息），
            页面部门选择会跳转到部门列表页面，选择完毕后会给员工页面data值上设置一个值，当返回员工列表页面后要在onshow的生命周期函数中接受此值，并将此值设置为空。
            页面岗位选择使用了自实现的多选控件multiPicker，具体用法请查看multiPicker目录下readme。


组织结构：
    组织结构列表页面展示为树形结构，该页面提供了父级别展开折叠子级数据的功能，实现为从接口拿到数据后，递归循环所有的子元素加上__open标记，
    当点击收缩展开时，递归查找点击的数据，把__open修改，具体实现请查看代码。
    当点击员工按钮时会把组织机构ID带到员工列表页面。

部门：
    部门列表页面会根据加载时的参数mode来设置页面模式，如果参数为select,则该页面会在对点击部门的时候进行选择的操作，将上一页面的data属性添加
    selectData属性，值为选择项的详情。
